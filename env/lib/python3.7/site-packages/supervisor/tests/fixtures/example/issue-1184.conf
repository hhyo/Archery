[unix_http_server]
file = /tmp/super.sock

[supervisorctl]
serverurl = unix:///tmp/super.sock

[supervisord]
pidfile = /tmp/super.pid

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface
